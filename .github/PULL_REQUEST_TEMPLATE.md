## What

## Why

## Checklist

- [ ] `cargo fmt --all` and `cargo clippy --workspace --all-targets` are clean
- [ ] `cargo test --workspace` passes (and `LSW_TEST_E2E=1` if wine/mingw are available)
- [ ] docs updated in the lsw-docs repo for any user-facing change
- [ ] no new code comments, ASCII only (repo convention)
