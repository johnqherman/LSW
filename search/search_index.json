{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"LSW","text":"<p>Build, run, debug, and ship Windows software without leaving Linux.</p> <pre><code>$ lsw setup\nDetected CMake project 'hello'\nCreated environment 'windows-x64' (llvm-mingw 22.1.8, wine 11.13)\n\n$ lsw run\nHello from LSW\n\n$ lsw check\n  + build            1 artifact(s) via CMake\n  + wine execution   1 test(s) passed under wine\n  + dependencies     all DLL imports resolved\n  + hardening        ASLR + DEP enabled\nall checks passed\n</code></pre>"},{"location":"#what-lsw-is","title":"What LSW is","text":"<p>LSW is one command-line tool for Windows development on Linux. It controls the tools that you already have. MinGW-w64 or Clang does the compilation. Wine runs the result. LSW adds the parts that these tools do not have:</p> <ul> <li>Isolated Wine environments, one for each target.</li> <li>A declarative project file, <code>lsw.toml</code>.</li> <li>A lock file, <code>lsw.lock</code>, for reproducible builds.</li> <li>Deterministic path mapping between the Linux view and the Windows view of   your project.</li> </ul> <p><code>lsw build</code> makes a genuine Windows PE binary from your CMake, Meson, Cargo, Zig, Make, or .NET project. <code>lsw run</code> starts the binary through Wine. <code>lsw test</code>, <code>lsw debug</code>, and <code>lsw package</code> complete the loop.</p> <p>A test that passes under Wine is not a test that passes on Windows. LSW keeps these two results separate at all times. Use <code>lsw verify --native</code> to run your binaries on a real Windows host and to get the real result.</p> <p>WSL puts a Linux userland on Windows. LSW points the other way: you develop for the Windows target, and you do not leave Linux.</p>"},{"location":"#where-to-go","title":"Where to go","text":"<ul> <li>Install - install LSW and the host tools.</li> <li>Quickstart - from zero to a passed   <code>lsw check</code> in five commands.</li> <li>Command reference - all commands and flags.</li> <li>Configuration - the full <code>lsw.toml</code>   schema and all <code>LSW_*</code> environment variables.</li> <li>Troubleshooting - the full <code>LSW####</code> error   catalogue and the usual first-run failures.</li> </ul>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Every LSW failure carries an <code>LSW####</code> code. <code>lsw explain &lt;code&gt;</code> prints the same summary and fix as this page. Start with <code>lsw doctor</code>: it checks the host, Wine, the cross toolchain, your build tools, and the active environment, and names anything missing.</p>"},{"location":"troubleshooting/#the-common-first-run-failures","title":"The common first-run failures","text":"<p><code>LSW1501</code> - wine not found. Install Wine from your package manager, or point <code>LSW_WINE</code> at a wine binary that is not on <code>PATH</code> (WineHQ <code>/opt</code> builds, Proton, Nix profiles).</p> <p><code>LSW1403</code> - no working cross toolchain. Install <code>mingw-w64</code> from your package manager, or download an llvm-mingw release, extract it, and set <code>LSW_TOOLCHAIN_DIRS=/path/to/llvm-mingw/bin</code>.</p> <p><code>LSW2011</code> - a build tool is missing. LSW drives your build system; for the default C/C++ path install <code>cmake</code> and <code>ninja</code>. <code>lsw doctor</code> lists exactly which tool a project needs.</p> <p>My existing CMake project will not configure. LSW hands CMake a cross toolchain file with <code>CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER</code> / <code>LIBRARY ONLY</code> / <code>INCLUDE ONLY</code>. <code>find_package</code> calls that expect host Linux libraries will (correctly) stop finding them; vendor Windows builds of those libraries with <code>lsw deps add</code>, or gate them out for the Windows target.</p> <p>A GUI test hangs in CI. Use <code>lsw test --headless</code> (runs under a virtual display via <code>xvfb-run</code>) and install <code>xvfb</code>.</p>"},{"location":"troubleshooting/#how-lsw-compares","title":"How LSW compares","text":"LSW cargo-xwin Docker + Wine image raw mingw + wine Languages C, C++, Rust, C#, Zig Rust only whatever the image has anything, by hand ABI GNU default, MSVC with your SDK MSVC image-dependent GNU Runs the result yes, isolated Wine prefixes no yes, in-container by hand Test/verify story <code>lsw test</code>, <code>lsw check</code>, native <code>lsw verify</code> none ad hoc ad hoc Packaging/signing zip, MSI, MSIX, sign none ad hoc ad hoc Reproducibility <code>lsw.lock</code> pins toolchain + wine Cargo.lock only image digest none Isolation prefixes + optional bwrap sandbox n/a container none <p>LSW does not replace your build system or toolchain; it configures the ones you already have behind one CLI.</p>"},{"location":"troubleshooting/#error-code-catalogue","title":"Error code catalogue","text":""},{"location":"troubleshooting/#lsw0000","title":"LSW0000","text":"<p>A command-line usage error (bad flags or arguments), not a build or environment failure.</p> <p>Fix: run the command with --help to see its accepted flags and arguments</p>"},{"location":"troubleshooting/#lsw1001","title":"LSW1001","text":"<p>A configuration or state file could not be read.</p> <p>Fix: check that the path named in the error exists and is readable</p>"},{"location":"troubleshooting/#lsw1002","title":"LSW1002","text":"<p>A configuration or state file could not be written.</p> <p>Fix: check permissions and free space for the path named in the error</p>"},{"location":"troubleshooting/#lsw1003","title":"LSW1003","text":"<p>A TOML file failed to parse.</p> <p>Fix: fix the syntax error at the file and position named in the error</p>"},{"location":"troubleshooting/#lsw1004","title":"LSW1004","text":"<p>Internal serialization of a configuration structure failed.</p> <p>Fix: this is an LSW bug; please report it with the full error message</p>"},{"location":"troubleshooting/#lsw1005","title":"LSW1005","text":"<p>No lsw.toml was found in this directory or any parent.</p> <p>Fix: run <code>lsw init</code> to scaffold a project, or cd into an existing one</p>"},{"location":"troubleshooting/#lsw1006","title":"LSW1006","text":"<p>The home directory could not be determined.</p> <p>Fix: set $HOME, or run as a user that has a home directory</p>"},{"location":"troubleshooting/#lsw1007","title":"LSW1007","text":"<p>The environment was created by a newer LSW than this build supports.</p> <p>Fix: upgrade LSW, or recreate the environment with <code>lsw env create --force</code></p>"},{"location":"troubleshooting/#lsw1201","title":"LSW1201","text":"<p>A host path passed to path translation was not absolute.</p> <p>Fix: canonicalize the path first, or join it onto the project root</p>"},{"location":"troubleshooting/#lsw1202","title":"LSW1202","text":"<p>No path mapping covers the given path.</p> <p>Fix: keep files under the project root or the environment's drive_c</p>"},{"location":"troubleshooting/#lsw1203","title":"LSW1203","text":"<p>The string is not a drive-letter Windows path.</p> <p>Fix: pass a form like C:\\dir\\file, as produced by <code>lsw path</code> or Windows tools</p>"},{"location":"troubleshooting/#lsw1204","title":"LSW1204","text":"<p>The path contains a non-UTF-8 component.</p> <p>Fix: rename the offending file or directory to valid UTF-8</p>"},{"location":"troubleshooting/#lsw1301","title":"LSW1301","text":"<p>The file could not be read for PE inspection.</p> <p>Fix: check that the file exists and is readable</p>"},{"location":"troubleshooting/#lsw1302","title":"LSW1302","text":"<p>The file has an MZ header but is not a valid PE image.</p> <p>Fix: the binary is corrupt or truncated; rebuild or re-download it</p>"},{"location":"troubleshooting/#lsw1303","title":"LSW1303","text":"<p>The file is not a PE executable.</p> <p>Fix: pass a Windows .exe or .dll, such as one produced by <code>lsw build</code></p>"},{"location":"troubleshooting/#lsw1401","title":"LSW1401","text":"<p>The requested toolchain provider is unavailable on this system.</p> <p>Fix: install the provider named in the error, or choose another with --toolchain</p>"},{"location":"troubleshooting/#lsw1402","title":"LSW1402","text":"<p>The toolchain provider failed its probe compile.</p> <p>Fix: read the probe output in the error; the toolchain cannot produce PE binaries</p>"},{"location":"troubleshooting/#lsw1403","title":"LSW1403","text":"<p>No toolchain provider produced a working Windows PE binary.</p> <p>Fix: install llvm-mingw or mingw-w64, then re-run <code>lsw env create</code></p>"},{"location":"troubleshooting/#lsw1404","title":"LSW1404","text":"<p>An unknown toolchain provider was requested.</p> <p>Fix: use a provider named in the error message, or omit --toolchain</p>"},{"location":"troubleshooting/#lsw1501","title":"LSW1501","text":"<p>The wine runtime was not found on PATH.</p> <p>Fix: install wine (e.g. <code>pacman -S wine</code> or <code>apt install wine</code>)</p>"},{"location":"troubleshooting/#lsw1502","title":"LSW1502","text":"<p>Wine prefix initialization failed.</p> <p>Fix: recreate the environment, or run <code>wineboot -u</code> with WINEPREFIX set to inspect</p>"},{"location":"troubleshooting/#lsw1503","title":"LSW1503","text":"<p>The runtime could not spawn a program.</p> <p>Fix: check that the binary named in the error exists and is executable</p>"},{"location":"troubleshooting/#lsw1504","title":"LSW1504","text":"<p>Runtime execution failed.</p> <p>Fix: read the detail in the error; adjust WINEDEBUG for more diagnostics</p>"},{"location":"troubleshooting/#lsw1505","title":"LSW1505","text":"<p>A strict sandbox was requested but bubblewrap is not installed.</p> <p>Fix: install bubblewrap, or drop --sandbox</p>"},{"location":"troubleshooting/#lsw1506","title":"LSW1506","text":"<p>A virtual display was requested but xvfb-run is not installed.</p> <p>Fix: install xvfb, or run with a real $DISPLAY</p>"},{"location":"troubleshooting/#lsw1507","title":"LSW1507","text":"<p>The process is not running in this environment.</p> <p>Fix: list processes with <code>lsw ps</code> to get a valid pid</p>"},{"location":"troubleshooting/#lsw1508","title":"LSW1508","text":"<p>The LSW_WINE override does not point at an executable wine binary.</p> <p>Fix: set LSW_WINE to a real wine executable, or unset it to use wine from PATH</p>"},{"location":"troubleshooting/#lsw2001","title":"LSW2001","text":"<p>No active environment is selected for this project.</p> <p>Fix: run <code>lsw use &lt;name&gt;</code> (or <code>lsw env create &lt;name&gt;</code> first)</p>"},{"location":"troubleshooting/#lsw2002","title":"LSW2002","text":"<p>The named environment does not exist.</p> <p>Fix: create it with <code>lsw env create &lt;name&gt;</code>, or list with <code>lsw env list</code></p>"},{"location":"troubleshooting/#lsw2003","title":"LSW2003","text":"<p>An environment with that name already exists.</p> <p>Fix: remove it with <code>lsw env remove &lt;name&gt;</code>, or choose another name</p>"},{"location":"troubleshooting/#lsw2004","title":"LSW2004","text":"<p>The target is not something LSW can execute.</p> <p>Fix: pass a PE/ELF/script, or force a domain with --host or --windows</p>"},{"location":"troubleshooting/#lsw2005","title":"LSW2005","text":"<p>The build command failed.</p> <p>Fix: re-run <code>lsw build --verbose</code> and read the compiler output above</p>"},{"location":"troubleshooting/#lsw2006","title":"LSW2006","text":"<p>Lsw.lock does not match the active environment.</p> <p>Fix: refresh the pins with <code>lsw build --update-lock</code>, or <code>lsw env restore</code></p>"},{"location":"troubleshooting/#lsw2007","title":"LSW2007","text":"<p>No build system was detected.</p> <p>Fix: add CMakeLists.txt, Cargo.toml, meson.build, build.zig, a .csproj, or set [build] command in lsw.toml</p>"},{"location":"troubleshooting/#lsw2008","title":"LSW2008","text":"<p>The target os in lsw.toml is not supported.</p> <p>Fix: set os = \"windows\"; LSW only targets Windows</p>"},{"location":"troubleshooting/#lsw2009","title":"LSW2009","text":"<p>Project scaffolding failed.</p> <p>Fix: read the detail in the error; check the directory is writable</p>"},{"location":"troubleshooting/#lsw2010","title":"LSW2010","text":"<p>An io operation failed.</p> <p>Fix: check the named path: existence, permissions, free space</p>"},{"location":"troubleshooting/#lsw2011","title":"LSW2011","text":"<p>A required external tool was not found on PATH.</p> <p>Fix: install the tool named in the error message</p>"},{"location":"troubleshooting/#lsw2012","title":"LSW2012","text":"<p>An invalid environment or project name was given.</p> <p>Fix: use a name without slashes, dots-only, or control characters</p>"},{"location":"troubleshooting/#lsw2013","title":"LSW2013","text":"<p>The build produced a host binary instead of a Windows PE binary.</p> <p>Fix: use the generated CMake toolchain, or make [build] honor CC/CXX/CFLAGS/LDFLAGS</p>"},{"location":"troubleshooting/#lsw2014","title":"LSW2014","text":"<p>No tests were found to run.</p> <p>Fix: add add_test(...) to CMakeLists.txt, or set [test] command in lsw.toml</p>"},{"location":"troubleshooting/#lsw2015","title":"LSW2015","text":"<p>A registry operation failed.</p> <p>Fix: check the key path (e.g. HKCU\\Software\\Example\\App) and the output above</p>"},{"location":"troubleshooting/#lsw2016","title":"LSW2016","text":"<p>The process does not belong to this environment or already exited.</p> <p>Fix: list this environment's processes with <code>lsw ps</code></p>"},{"location":"troubleshooting/#lsw2017","title":"LSW2017","text":"<p>The build directory was configured without the Windows test emulator.</p> <p>Fix: remove the build/ directory and re-run <code>lsw test</code></p>"},{"location":"troubleshooting/#lsw2018","title":"LSW2018","text":"<p>Two build artifacts share the same file name.</p> <p>Fix: rename a target, or package a single configuration</p>"},{"location":"troubleshooting/#lsw2019","title":"LSW2019","text":"<p>An SDK with that name is already imported.</p> <p>Fix: re-import with <code>lsw sdk import &lt;name&gt; --force</code>, or <code>lsw sdk remove &lt;name&gt;</code></p>"},{"location":"troubleshooting/#lsw2020","title":"LSW2020","text":"<p>The named SDK is not imported.</p> <p>Fix: list imported SDKs with <code>lsw sdk list</code></p>"},{"location":"troubleshooting/#lsw2021","title":"LSW2021","text":"<p>An unsupported verification transport was configured.</p> <p>Fix: set transport to \"ssh\", \"winrm\", or \"https\" in [verify]</p>"},{"location":"troubleshooting/#lsw2022","title":"LSW2022","text":"<p>A provider plugin violated the plugin protocol.</p> <p>Fix: update or fix the plugin named in the error</p>"},{"location":"troubleshooting/#lsw2023","title":"LSW2023","text":"<p>The optional lsw daemon is not running.</p> <p>Fix: start it with <code>lswd</code>; most commands work without the daemon</p>"},{"location":"troubleshooting/#lsw2024","title":"LSW2024","text":"<p>An unsafe path or name was passed to native verification.</p> <p>Fix: use a drive-letter path with segments of [A-Za-z0-9._+-] only</p>"},{"location":"troubleshooting/#lsw2025","title":"LSW2025","text":"<p>Rust has no GNU-ABI Windows target for this arch.</p> <p>Fix: use x86_64, x86, or aarch64 for Rust projects</p>"},{"location":"troubleshooting/#lsw2026","title":"LSW2026","text":"<p>A Windows service operation failed.</p> <p>Fix: read the operation detail in the error message</p>"},{"location":"troubleshooting/#lsw2027","title":"LSW2027","text":"<p>The compatibility database could not be read or queried.</p> <p>Fix: read the detail in the error message</p>"},{"location":"troubleshooting/#lsw2028","title":"LSW2028","text":"<p>The debug adapter hit a protocol error.</p> <p>Fix: restart the debug session; read the detail in the error message</p>"},{"location":"troubleshooting/#lsw2029","title":"LSW2029","text":"<p>MSIX signing failed.</p> <p>Fix: check the signing certificate and key; read the detail in the error</p>"},{"location":"troubleshooting/#lsw2030","title":"LSW2030","text":"<p>An invalid [sandbox] network value was set.</p> <p>Fix: use network = \"host\", \"isolated\", or \"none\"</p>"},{"location":"troubleshooting/#lsw2031","title":"LSW2031","text":"<p>The crash dump could not be parsed.</p> <p>Fix: pass a Windows minidump (.dmp), such as one written by the runtime</p>"},{"location":"troubleshooting/#lsw2032","title":"LSW2032","text":"<p>The native import probe failed on the remote host.</p> <p>Fix: check ssh connectivity to the host named in the error</p>"},{"location":"troubleshooting/#lsw2033","title":"LSW2033","text":"<p>The package was not found in the mingw-w64 package set.</p> <p>Fix: use the upstream library name (e.g. zlib, sqlite3, libpng)</p>"},{"location":"troubleshooting/#lsw2034","title":"LSW2034","text":"<p>A download failed.</p> <p>Fix: check network access and the URL in the error, then retry</p>"},{"location":"troubleshooting/#lsw2035","title":"LSW2035","text":"<p>A downloaded file failed checksum verification.</p> <p>Fix: retry the download; if it persists the mirror is serving a bad file</p>"},{"location":"troubleshooting/#lsw2036","title":"LSW2036","text":"<p>An archive could not be unpacked.</p> <p>Fix: retry the download; the archive may be corrupt</p>"},{"location":"troubleshooting/#lsw2037","title":"LSW2037","text":"<p>No mingw-w64 package repository exists for this arch.</p> <p>Fix: use an arch with a mingw-w64 repo, or vendor the dependency manually</p>"},{"location":"troubleshooting/#lsw2038","title":"LSW2038","text":"<p>Cross-architecture execution needs a Wine build for the target arch.</p> <p>Fix: set the env var named in the error to a matching wine (under qemu)</p>"},{"location":"troubleshooting/#lsw2039","title":"LSW2039","text":"<p>Case = \"strict\" found case-insensitive file name collisions.</p> <p>Fix: rename the colliding files, or set [filesystem] case = \"native\"</p>"},{"location":"troubleshooting/#lsw2040","title":"LSW2040","text":"<p>The MSI failed install/uninstall verification in a scratch environment.</p> <p>Fix: inspect the msiexec output in the error; rerun <code>lsw package --target msi --verify</code></p>"},{"location":"troubleshooting/#lsw2041","title":"LSW2041","text":"<p>C# NativeAOT cross-compilation prerequisites are missing.</p> <p>Fix: needs x86_64 target, clang, lld-link, and a mingw-w64 sysroot; see the error detail</p>"},{"location":"troubleshooting/#lsw2042","title":"LSW2042","text":"<p>The SDK import failed.</p> <p>Fix: pass --from \\ pointing at an SDK directory; see the error detail"},{"location":"troubleshooting/#lsw2043","title":"LSW2043","text":"<p>A custom [filesystem] project_drive/mount_project was set but is not supported.</p> <p>Fix: use project_drive = \"C:\" and mount_project = \"/src\" (the project mounts at C:\\src\\)"},{"location":"troubleshooting/#lsw2044","title":"LSW2044","text":"<p>Env restore cannot reconstruct an SDK/MSVC toolchain (lsw.lock has no SDK identity).</p> <p>Fix: re-import the SDK and recreate the environment with <code>lsw env create &lt;name&gt; --sdk &lt;name&gt;</code></p>"},{"location":"troubleshooting/#lsw2045","title":"LSW2045","text":"<p>The project or environment path resolves to a system directory, so strict sandbox isolation refuses to bind it.</p> <p>Fix: move the project out of the filesystem root or a system path, or relax [sandbox] isolation</p>"},{"location":"troubleshooting/#lsw2046","title":"LSW2046","text":"<p>The --system value is not a build system LSW knows.</p> <p>Fix: use cmake, cargo, make, ninja, meson, zig, dotnet, or explicit with a [build] command</p>"},{"location":"guide/analysis/","title":"Binary analysis","text":"<p>The analysis commands accept an explicit file. When you give no file, LSW builds the project and uses its artifact.</p>"},{"location":"guide/analysis/#examine-a-binary","title":"Examine a binary","text":"<p><code>lsw inspect [pe]</code> shows the format, the machine, the subsystem, the entry point, the sections, the embedded resources (manifest, version information, icon), and the imports. For each imported DLL, it shows if the DLL is available in the runtime.</p> <p><code>lsw audit [pe]</code> reports the security hardening of the binary: ASLR, DEP, CFG, SafeSEH, and signing. Each check is enabled, disabled, or not applicable to the target architecture. The baseline verdict needs ASLR and DEP.</p> <p><code>lsw exports [dll]</code> lists the exported symbols of a DLL.</p>"},{"location":"guide/analysis/#dependencies","title":"Dependencies","text":"<p><code>lsw deps tree [pe]</code> shows the transitive DLL tree of a binary. Each node is resolved, system, or missing. The search covers the artifact directory, the toolchain sysroot, and the prefix <code>system32</code>.</p>"},{"location":"guide/analysis/#compare-and-measure","title":"Compare and measure","text":"<p><code>lsw diff &lt;a.exe&gt; &lt;b.exe&gt;</code> compares two binaries: imports, exports, sections, and size.</p> <p><code>lsw size [pe]</code> breaks the size down by section bucket: code, data, resources, and others.</p> <ul> <li><code>--baseline &lt;pe&gt;</code> compares against an earlier build.</li> <li><code>--max-growth &lt;pct&gt;</code> fails the command when a bucket grows more than   this percentage. Use this as a CI gate.</li> </ul> <p><code>lsw strings [file] [--min &lt;n&gt;]</code> lists the printable ASCII and UTF-16 strings in a file.</p>"},{"location":"guide/analysis/#software-bill-of-materials","title":"Software bill of materials","text":"<p><code>lsw sbom [pe]</code> writes a CycloneDX SBOM. The components are the imported DLLs, the <code>[dependencies]</code> packages with their SHA-256 checksums from <code>lsw.lock</code>, and the toolchain identity.</p>"},{"location":"guide/analysis/#crash-dumps","title":"Crash dumps","text":"<p><code>lsw crash &lt;dump.dmp&gt;</code> decodes a Windows minidump. It shows the exception, the crash address, the faulting module and offset, the thread, and the platform. <code>lsw crash --force &lt;pe&gt;</code> launches the program under winedbg and forces a dump without a crash.</p>"},{"location":"guide/ci/","title":"CI","text":""},{"location":"guide/ci/#generate-a-pipeline","title":"Generate a pipeline","text":"<ul> <li><code>lsw ci init github</code> writes <code>.github/workflows/lsw.yml</code>.</li> <li><code>lsw ci init gitlab</code> writes <code>.gitlab-ci.yml</code>.</li> </ul> <p>Both pipelines do these steps:</p> <ol> <li>Install Wine, MinGW, and the build tools.</li> <li>Cache the compiled <code>lsw</code> binary, keyed on its version.</li> <li>Cache the environments and the package downloads, keyed on <code>lsw.lock</code>.</li> <li>Run <code>lsw setup</code>, <code>lsw build</code>, and <code>lsw test --headless</code>.</li> </ol> <p>The GitHub workflow also has two dispatch-gated jobs: reproducibility verification (<code>lsw verify --reproducible</code>) and artifact signing from a repository-secret PFX certificate.</p>"},{"location":"guide/ci/#parts-for-your-own-pipeline","title":"Parts for your own pipeline","text":"<ul> <li><code>lsw test --junit report.xml</code> - a test report for CI ingestion.</li> <li><code>lsw test --headless</code> - GUI tests under a virtual display.</li> <li><code>lsw env export</code> and <code>lsw env import-archive</code> - move a prepared   environment through the CI cache.</li> <li><code>lsw env restore &lt;name&gt;</code> - rebuild from <code>lsw.lock</code>, and fail loudly on   drift.</li> <li><code>lsw size --baseline &lt;pe&gt; --max-growth &lt;pct&gt;</code> - a size-regression gate.</li> <li><code>--format json</code> on report commands - machine-readable output.</li> <li><code>lsw verify --native</code> - the real Windows verdict, when the runner can   reach a <code>[verify]</code> host.</li> </ul>"},{"location":"guide/compat/","title":"Compatibility and tracing","text":"<p>Wine does not implement all of the Windows API. These commands measure what your binary uses and what the runtime supports. The results are scoped: a local number describes the Wine runtime, not native Windows.</p>"},{"location":"guide/compat/#measure-compatibility","title":"Measure compatibility","text":"<p><code>lsw compat [pe]</code> builds the project, runs the binary, and produces a measured report:</p> <ul> <li>The imported DLLs and the imported API functions.</li> <li>The runtime trace of the execution.</li> <li>The API functions that the runtime did not support.</li> </ul> <p>Options:</p> <ul> <li><code>--db</code> - record the results in the local compatibility database.</li> <li><code>--native</code> - also run the binary on the <code>[verify]</code> Windows host and add   the real Windows verdict.</li> </ul>"},{"location":"guide/compat/#query-past-results","title":"Query past results","text":"<p><code>lsw compat-query &lt;dll&gt;</code> or <code>lsw compat-query &lt;module!function&gt;</code> queries the compatibility database. The database is local to your machine, in <code>~/.local/share/lsw/compat-db.json</code>. LSW never uploads it.</p>"},{"location":"guide/compat/#trace-an-execution","title":"Trace an execution","text":"<p><code>lsw trace [pe]</code> runs the binary and shows what happened:</p> <ul> <li>The DLL loads.</li> <li>The registry and filesystem access.</li> <li>The unsupported API calls.</li> <li>An event timeline.</li> </ul> <p>Options:</p> <ul> <li><code>--relay</code> - include the full API relay log. This is verbose.</li> <li><code>--filter &lt;glob&gt;</code> - limit the events to matching names.</li> <li><code>--format csv</code> or <code>--format json</code> - machine-readable timeline output.</li> </ul>"},{"location":"guide/editors/","title":"Editor integration","text":"<p>The editor packages are thin layers over the <code>lsw</code> CLI. The CLI stays the source of truth. Each package starts <code>lsw</code> commands, reads <code>lsw ide env</code> to configure the language tools, and starts <code>lsw dap</code> for debugging.</p>"},{"location":"guide/editors/#vs-code","title":"VS Code","text":"<p>Install the extension:</p> <pre><code>code --install-extension lsw.lsw\n</code></pre> <p>The extension gives you:</p> <ul> <li>Commands: LSW: Setup / Init / Build / Run / Test / Doctor / Verify   and LSW: Configure C/C++ IntelliSense (this writes   <code>.vscode/c_cpp_properties.json</code> from <code>lsw ide env</code>).</li> <li>Tasks for build, test, check, and package, with a problem matcher, so   build errors are clickable.</li> <li>An <code>lsw</code> debug type that runs <code>lsw dap</code>. <code>lsw ide launch-config</code> writes   a ready <code>.vscode/launch.json</code>, or add an entry yourself:</li> </ul> <pre><code>{ \"type\": \"lsw\", \"request\": \"launch\", \"name\": \"Debug PE\",\n  \"program\": \"${workspaceFolder}/build/${workspaceFolderBasename}.exe\" }\n</code></pre>"},{"location":"guide/editors/#neovim","title":"Neovim","text":"<p>Point your plugin manager at the <code>editors/nvim</code> directory of the repository, then call:</p> <pre><code>require(\"lsw\").setup({ path = \"lsw\" })\n</code></pre> <p>The plugin supplies <code>:LswBuild</code>, <code>:LswTest</code>, <code>:LswVerify</code>, <code>:LswRun [program]</code>, <code>:LswConfigure</code> (this reads <code>lsw ide env</code> into <code>vim.g.lsw_*</code>), and <code>:LswEnv</code>. When nvim-dap is installed, the plugin also adds an <code>lsw</code> adapter and a launch configuration.</p>"},{"location":"guide/editors/#jetbrains-ides","title":"JetBrains IDEs","text":"<p><code>editors/jetbrains/lsw-tools.xml</code> is an External Tools definition. Copy it to the <code>tools</code> configuration directory of the IDE, for example <code>~/.config/JetBrains/&lt;Product&gt;&lt;Version&gt;/tools/</code>. Or create the entries under Settings &gt; Tools &gt; External Tools. It adds the actions LSW Build / Test / Verify / Run for the project directory.</p>"},{"location":"guide/editors/#any-other-editor","title":"Any other editor","text":"<p>Two stable interfaces cover the rest:</p> <ul> <li><code>lsw ide env</code> prints the target, the compiler, the sysroot, the include   paths, the defines, and the Wine prefix, as JSON.</li> <li><code>lsw dap</code> is a Debug Adapter Protocol server on stdio.</li> </ul>"},{"location":"guide/environments/","title":"Environments and reproducibility","text":"<p>An environment is an isolated Wine prefix plus a probed toolchain. Each environment lives in <code>~/.local/share/lsw/environments/&lt;name&gt;/</code>. A project selects its environment with <code>lsw use &lt;name&gt;</code>. The selection is recorded in <code>lsw.toml</code>.</p> <p>The global <code>--env &lt;name&gt;</code> flag overrides the selection for one command. It also lets <code>ps</code>, <code>kill</code>, <code>registry</code>, and <code>service</code> work outside a project directory.</p>"},{"location":"guide/environments/#manage-environments","title":"Manage environments","text":"<ul> <li><code>lsw env create &lt;name&gt; [--arch &lt;arch&gt;] [--toolchain &lt;id&gt;] [--sdk &lt;name&gt;]   [--expose-home] [--force]</code> - create an environment.</li> <li><code>lsw env list</code> - list them, with health status.</li> <li><code>lsw env clone &lt;src&gt; &lt;dst&gt;</code> - fast reflink copy.</li> <li><code>lsw env remove &lt;name&gt;</code> - delete an environment and its prefix.</li> <li><code>lsw env provision winetricks &lt;verbs...&gt;</code> - run winetricks in the   prefix. Examples: <code>dxvk</code>, <code>vkd3d</code>, <code>corefonts</code>.</li> </ul>"},{"location":"guide/environments/#move-environments-between-machines","title":"Move environments between machines","text":"<p><code>lsw env export &lt;name&gt; &lt;file.tar.zst&gt;</code> packs an environment into one archive. <code>lsw env import-archive &lt;name&gt; &lt;file&gt;</code> restores it. Use this to move a prepared environment through a CI cache, instead of a <code>wineboot</code> run on each job.</p>"},{"location":"guide/environments/#the-lock-file","title":"The lock file","text":"<p><code>lsw.lock</code> pins the build inputs:</p> <ul> <li>The toolchain: the SHA-256 checksum of the compiler.</li> <li>The runtime: the SHA-256 checksum of the Wine binary, and its version.</li> <li>The sysroot fingerprint.</li> <li>Each <code>[dependencies]</code> package: its version and its SHA-256 checksum.</li> </ul> <p>When the environment drifts from the pins, the build fails with <code>LSW2006</code>. To accept the new state, run <code>lsw build --update-lock</code>. To rebuild an environment from the pins, run <code>lsw env restore &lt;name&gt;</code>. It verifies the result against the lock file. Commit both <code>lsw.toml</code> and <code>lsw.lock</code> to version control.</p>"},{"location":"guide/install/","title":"Install","text":""},{"location":"guide/install/#install-lsw","title":"Install LSW","text":"<pre><code>cargo install lsw &amp;&amp; lsw install\n</code></pre> <p><code>cargo install lsw</code> puts the <code>lsw</code> and <code>lswd</code> binaries on your <code>PATH</code>. <code>lsw install</code> adds shell completions (bash, zsh, fish) and man pages. The shell integration is optional. To install only the binaries, run <code>cargo install lsw</code> alone.</p>"},{"location":"guide/install/#install-the-host-tools","title":"Install the host tools","text":"<p>LSW controls tools that are on your machine. It does not replace them. You need:</p> <ul> <li>Wine, version 9 or later. <code>lsw doctor</code> shows a warning for older   versions.</li> <li>A MinGW-w64 or llvm-mingw cross toolchain.</li> <li>Your build system.</li> </ul> <p>For the default C/C++ path, install these packages:</p> <pre><code># Ubuntu / Debian\nsudo apt install wine wine64 mingw-w64 cmake ninja-build\n\n# Fedora\nsudo dnf install wine mingw64-gcc mingw64-gcc-c++ cmake ninja-build\n\n# Arch\nsudo pacman -S wine mingw-w64-gcc cmake ninja\n\n# Nix (get the toolchain with: lsw toolchain install)\nnix-shell -p wineWowPackages.stable cmake ninja\n</code></pre> <p>For other languages:</p> <ul> <li>Rust projects need <code>rustup</code> with the <code>x86_64-pc-windows-gnu</code> target.</li> <li>.NET projects need the <code>dotnet</code> SDK.</li> <li>Zig projects need <code>zig</code>.</li> </ul> <p>Some features need more tools. Install them only when you use the feature:</p> Feature Tools Headless GUI tests <code>xvfb-run</code> <code>lsw run --sandbox strict</code> <code>bubblewrap</code> <code>lsw package --target msi</code> <code>msitools</code> (<code>wixl</code>) <code>lsw package --target msix</code>, <code>lsw sign</code> <code>zip</code>, <code>osslsigncode</code>, <code>openssl</code> <code>lsw package --target nsis</code> <code>makensis</code> <code>lsw deps add</code> <code>curl</code>, <code>tar</code> Cross-family emulation <code>qemu-user</code>"},{"location":"guide/install/#what-lsw-downloads","title":"What LSW downloads","text":"<p>LSW ships only its own source and binaries. It does not ship Wine, MinGW, or Microsoft SDK content. You supply those tools. LSW does not download files without your command. Three commands download content, and only when you run them:</p> <ul> <li><code>lsw deps add</code> (MSYS2 package repositories)</li> <li><code>lsw toolchain install</code> (llvm-mingw releases from GitHub)</li> <li><code>lsw sdk acquire</code> (Microsoft SDK content through xwin)</li> </ul> <p>See SECURITY.md for the full list and the trust model.</p>"},{"location":"guide/install/#check-the-setup","title":"Check the setup","text":"<p>Run <code>lsw doctor</code>. It examines the host, Wine, the toolchain, your build tools, and the active environment. It names each missing item.</p> <p>If your Wine binary is not on <code>PATH</code>, set <code>LSW_WINE</code> to its location. Examples: WineHQ builds in <code>/opt</code>, Proton, and Nix profiles.</p>"},{"location":"guide/languages/","title":"Languages and build systems","text":"<p>LSW examines the project directory and selects the build system. The detection order is: CMake, Meson, Zig, Cargo, .NET, Ninja, Make. To force a different system, use <code>lsw build --system &lt;name&gt;</code>. To use your own command, set <code>[build] command</code> in <code>lsw.toml</code>.</p>"},{"location":"guide/languages/#c-and-c","title":"C and C++","text":"<p>C and C++ projects build with CMake, Meson, Ninja, or Make.</p> <ul> <li>LSW generates a cross-toolchain file for CMake and a cross file for   Meson.</li> <li>LSW exports the cross <code>CC</code>, <code>CXX</code>, <code>CFLAGS</code>, and <code>LDFLAGS</code> to all build   systems.</li> <li>Set <code>[toolchain] ccache = true</code> to put ccache in front of CMake   compilation.</li> </ul> <p>Build commands also receive these variables: <code>LSW_ENV</code> (the environment name), <code>LSW_PROJECT</code> (the project root), and <code>LSW_TARGET_FLAGS</code> (the cross C flags).</p>"},{"location":"guide/languages/#rust","title":"Rust","text":"<p>LSW detects <code>Cargo.toml</code> automatically. Rust is a first-class language.</p> <pre><code>lsw rust init hello-rs &amp;&amp; cd hello-rs\nlsw setup &amp;&amp; lsw build      # cargo build --target &lt;arch&gt;-pc-windows-gnu\nlsw test                    # cargo test, run under Wine\nlsw rust doctor             # show the Rust-&gt;Windows readiness\n</code></pre> <p><code>lsw test</code> sets a Cargo target runner, so the test binaries run under Wine.</p>"},{"location":"guide/languages/#c-and-net","title":"C# and .NET","text":"<p>LSW detects <code>.csproj</code>, <code>.sln</code>, and <code>.fsproj</code> files automatically.</p> <pre><code>lsw dotnet init hello-cs &amp;&amp; cd hello-cs\nlsw setup &amp;&amp; lsw build      # dotnet publish -r &lt;rid&gt; --self-contained\nlsw run                     # runs under Wine\nlsw dotnet doctor           # show the C#-&gt;Windows readiness\n</code></pre> <p>Builds are self-contained. The binary runs under Wine without a .NET runtime in the prefix. LSW ships no runtime.</p> <p>WPF and WinForms run badly under Wine. The supported path is console applications and service applications. Use <code>lsw verify --native</code> to get the real Windows result.</p>"},{"location":"guide/languages/#nativeaot","title":"NativeAOT","text":"<p><code>lsw build --aot</code> compiles C# with NativeAOT. The output is a native PE binary: smaller, faster to start, with no CLR. The full <code>inspect</code>, <code>audit</code>, and <code>compat</code> tools apply to it.</p> <p>The .NET SDK does not permit cross-OS NativeAOT. LSW supplies the MSVC CRT glue itself and links with <code>lld-link</code>. This works from Linux without a Microsoft SDK. The host needs <code>clang</code> and <code>lld-link</code>. The scope is x86_64 console applications. <code>lsw dotnet doctor</code> shows the NativeAOT row.</p>"},{"location":"guide/languages/#zig","title":"Zig","text":"<p>LSW detects <code>build.zig</code> automatically. <code>lsw build</code> runs <code>zig build</code> for the Windows target of the environment.</p>"},{"location":"guide/languages/#prebuilt-windows-libraries","title":"Prebuilt Windows libraries","text":"<p><code>lsw deps add &lt;name&gt;</code> installs a mingw-w64 library from the MSYS2 package repositories into the project <code>deps/</code> sysroot. The download is verified against its SHA-256 checksum. LSW records the package in <code>[dependencies]</code> in <code>lsw.toml</code> and pins it in <code>lsw.lock</code>. Builds find the include and library paths automatically.</p> <ul> <li><code>lsw deps list</code> shows the installed libraries.</li> <li><code>lsw deps remove &lt;name&gt;</code> removes one.</li> <li><code>lsw deps vendor &lt;dir&gt;</code> copies a prebuilt <code>include/</code>, <code>lib/</code>, <code>bin/</code>   tree into the same sysroot. Use this for vcpkg or Conan exports.</li> </ul>"},{"location":"guide/packaging/","title":"Packaging and signing","text":"<p><code>lsw package</code> turns the build output into a distributable:</p> <pre><code>lsw package --target portable-directory   # dist/&lt;name&gt;-&lt;arch&gt;/\nlsw package --target zip                  # + .zip\nlsw package --target msi                  # Windows Installer (needs wixl/msitools)\nlsw package --target msix                 # signed MSIX (needs zip, osslsigncode, openssl)\nlsw package --target nsis                 # NSIS setup.exe (needs makensis)\nlsw package --target winget               # MSI + winget manifests\n</code></pre> <p><code>--bundle-deps</code> copies the non-system DLL closure of each artifact into the package.</p>"},{"location":"guide/packaging/#package-metadata","title":"Package metadata","text":"<p>Add a <code>[package]</code> section to <code>lsw.toml</code>:</p> <pre><code>[package]\nversion       = \"1.2.0\"\npublisher     = \"Acme Corp\"\ndescription   = \"Example application\"\nicon          = \"assets/app.ico\"\nshortcuts     = true\n</code></pre> <p>The metadata feeds the MSI, MSIX, NSIS, and winget outputs. LSW also embeds it into the built PE binary: the icon, a VERSIONINFO resource, and an application manifest (<code>dpi_aware</code>, <code>requires_admin</code>). See the configuration reference for all keys.</p> <p>With <code>shortcuts = true</code>, the MSI and NSIS installers create a Start-menu shortcut for each executable.</p> <p>The winget target needs <code>installer_url</code>: the URL where you will host the installer. LSW writes the three manifest files with the installer's SHA-256 checksum.</p>"},{"location":"guide/packaging/#verify-the-installer","title":"Verify the installer","text":"<p><code>lsw package --target msi --verify</code> tests the MSI before it reports success:</p> <ol> <li>Clone the active environment to a scratch prefix.</li> <li>Install the MSI quietly with <code>msiexec /i</code>.</li> <li>Make sure that each packaged file is under Program Files.</li> <li>Uninstall with <code>msiexec /x</code>.</li> <li>Make sure that no files remain.</li> </ol> <p>A failure shows as <code>LSW2040</code> with the msiexec output.</p>"},{"location":"guide/packaging/#signing","title":"Signing","text":"<p>MSIX packages are built natively (manifest, block map, OPC zip) and signed with a cached self-signed identity.</p> <p><code>lsw sign &lt;pe&gt;</code> signs one binary:</p> <ul> <li>Default: the self-signed development identity.</li> <li><code>--pfx cert.pfx --pfx-pass-env VAR</code>: a real PKCS#12 certificate. The   passphrase comes from the environment variable, never from a flag.</li> <li><code>--timestamp-url &lt;url&gt;</code>: RFC3161 timestamping, so the signature outlives   the certificate.</li> </ul> <p><code>lsw sign &lt;pe&gt; --verify</code> checks an existing signature with osslsigncode.</p> <p>Note: self-signed binaries install only where the certificate is trusted, or in Windows developer mode.</p>"},{"location":"guide/packaging/#reproducible-builds","title":"Reproducible builds","text":"<p><code>lsw build --reproducible</code> makes byte-identical artifacts. It passes <code>-Wl,--no-insert-timestamp</code> to the linker and sets the PE <code>TimeDateStamp</code> to zero. <code>lsw verify --reproducible</code> proves the property: it builds two times and compares.</p>"},{"location":"guide/quickstart/","title":"Quickstart","text":""},{"location":"guide/quickstart/#start-in-an-existing-project","title":"Start in an existing project","text":"<p>LSW detects CMake, Meson, Cargo, Zig, Make, and .NET projects.</p> <ol> <li>Go to the project directory.</li> <li>Run <code>lsw setup</code>. It detects the project, creates a default environment,    and writes <code>lsw.toml</code>.</li> <li>Run <code>lsw build</code>. It cross-compiles to <code>build/&lt;name&gt;.exe</code>, a real PE    binary.</li> <li>Run <code>lsw run</code>. It builds and runs the binary through Wine.</li> <li>Run <code>lsw check</code>. It validates the full pipeline: configuration,    environment, build, Wine execution, DLL resolution, and PE hardening.</li> </ol> <pre><code>lsw setup\nlsw build\nlsw run\nlsw check\nlsw package\n</code></pre>"},{"location":"guide/quickstart/#start-from-nothing","title":"Start from nothing","text":"<ol> <li>Run <code>lsw init hello</code>. It scaffolds <code>lsw.toml</code> and a CMake hello    project.</li> <li>Go into the new directory.</li> <li>Run <code>lsw setup</code>, then <code>lsw check</code>.</li> </ol> <pre><code>lsw init hello &amp;&amp; cd hello\nlsw setup\nlsw check\n</code></pre> <p><code>lsw init --template &lt;name&gt;</code> selects the scaffold:</p> Template Result <code>console</code> C console program (default) <code>cpp</code> C++ console program <code>gui</code> WinMain window program <code>dll</code> Shared library <code>service</code> Windows service skeleton <p>Complete example projects are in <code>examples/</code>. Copy one, or run it in place with <code>lsw setup &amp;&amp; lsw check</code>. The examples gitignore <code>lsw.lock</code> because they float with the LSW version. In your own project, commit both <code>lsw.toml</code> and <code>lsw.lock</code>.</p>"},{"location":"guide/quickstart/#the-next-commands","title":"The next commands","text":"<ul> <li><code>lsw test</code> - run the test suite under Wine.</li> <li><code>lsw inspect</code> and <code>lsw audit</code> - examine the built PE binary.</li> <li><code>lsw doctor</code> - examine the host setup.</li> <li><code>lsw watch</code> - build again on each source change.</li> <li><code>lsw verify --native</code> - run the binaries on a real Windows host.</li> </ul> <p>For more environments, other architectures, and the MSVC ABI, use <code>lsw env create</code> and <code>lsw use</code> directly. <code>lsw setup</code> is a shortcut over the same machinery.</p>"},{"location":"guide/quickstart/#the-command-map","title":"The command map","text":"<pre><code>Getting started:  setup  init  doctor  check\nBuild / run:      build  run  exec  test  shell  watch  clean\nAnalysis:         inspect  audit  exports  deps  diff  size  strings  sbom  crash\nCompatibility:    compat  compat-query  trace\nDebugging:        debug  dap\nShip:             package  sign  verify  ci\nEnvironments:     env  use  registry  ps  kill  service  sdk  toolchain  path\nLanguages:        rust  dotnet\nTooling:          ide  plugin  daemon  config  explain  completions  man  install\n</code></pre> <p>See the command reference for all flags. Most report commands accept <code>--format json</code>. When a command fails with an <code>LSW####</code> code, run <code>lsw explain &lt;code&gt;</code>, or see troubleshooting.</p>"},{"location":"guide/security/","title":"Sandboxing and security","text":"<p>The Wine prefix is a compatibility boundary. It is not a security boundary.</p> <p>By default, <code>lsw run</code> executes a program with your Linux privileges. The program can reach the host filesystem through Wine's <code>Z:</code> drive. The Windows user profile hides your host home directory, unless you created the environment with <code>--expose-home</code>.</p> <p>Treat <code>lsw run &lt;app.exe&gt;</code> like a native Linux program that you downloaded. Run only the binaries that you trust.</p>"},{"location":"guide/security/#the-strict-sandbox","title":"The strict sandbox","text":"<p>For real isolation, run <code>lsw run --sandbox strict &lt;app.exe&gt;</code>. It uses bubblewrap:</p> <ul> <li>The system directories are read-only.</li> <li>The home directory is masked.</li> <li>Only the environment and the project are writable.</li> </ul> <p>The <code>[sandbox]</code> section in <code>lsw.toml</code> adds limits:</p> <pre><code>[sandbox]\nnetwork     = \"none\"   # host | isolated (NAT via pasta) | none\ncpu_seconds = 30\nmemory_mb   = 2048\n</code></pre>"},{"location":"guide/security/#network-and-local-execution","title":"Network and local execution","text":"<p>LSW does not download files without your command. Three commands download content: <code>lsw deps add</code>, <code>lsw toolchain install</code>, and <code>lsw sdk acquire</code>. <code>lsw plugin list</code> executes <code>lsw-provider-*</code> binaries from your <code>PATH</code>.</p> <p>The full threat model and the private vulnerability-report channel are in SECURITY.md.</p>"},{"location":"guide/targets/","title":"Target ABIs and architectures","text":""},{"location":"guide/targets/#abis","title":"ABIs","text":"<p>LSW builds GNU-ABI binaries with MinGW-w64 by default. LSW can also build MSVC-ABI binaries with clang-cl and a Windows SDK. You supply the SDK. LSW does not redistribute SDK content.</p> <pre><code>lsw sdk acquire winsdk --accept-license # download through xwin\nlsw env create msvc --sdk winsdk        # clang-cl + lld-link, MSVC ABI\nlsw build                               # produces an MSVC-ABI PE\n</code></pre> <p>If you already have SDK content, use <code>lsw sdk import &lt;name&gt; --from &lt;dir&gt;</code>. An <code>xwin splat</code> directory and a flat <code>include/lib</code> layout are both accepted. <code>lsw sdk list</code> and <code>lsw sdk remove</code> manage the imported SDKs.</p>"},{"location":"guide/targets/#architectures","title":"Architectures","text":"<p>An environment targets one architecture: <code>x86_64</code> (the default), <code>x86</code>, <code>aarch64</code>, <code>armv7</code>, or <code>arm64ec</code>. Select it with <code>lsw env create --arch &lt;arch&gt;</code>, or set <code>[target] arch</code> in <code>lsw.toml</code> before <code>lsw setup</code>.</p> <p>LSW finds toolchains in this order:</p> <ol> <li>The directories in <code>$LSW_TOOLCHAIN_DIRS</code> (colon-separated).</li> <li>Managed installs under <code>~/.local/share/lsw/toolchains</code>, newest first.</li> <li><code>$PATH</code>.</li> </ol> <p>A self-contained llvm-mingw covers all five architectures and does not touch the system mingw-w64. <code>lsw toolchain install llvm-mingw</code> downloads and manages one for you:</p> <pre><code>lsw toolchain install llvm-mingw    # latest release\nlsw env create arm64 --arch aarch64\nlsw build                           # -&gt; build/&lt;name&gt;.exe (ARM64 PE)\n</code></pre> <p><code>lsw toolchain list</code> and <code>lsw toolchain remove</code> manage the installed toolchains. <code>lsw doctor</code> shows a support matrix for the project's architecture: the Rust target, the Zig target, the .NET RID, and the deps repository.</p>"},{"location":"guide/targets/#run-a-cross-family-binary","title":"Run a cross-family binary","text":"<p>A same-family binary (an <code>x86</code> PE on an <code>x86_64</code> host) runs directly.</p> <p>A cross-family binary (an <code>aarch64</code> PE on an <code>x86_64</code> host) runs through qemu user-mode emulation. Prepare these items:</p> <ol> <li>Install the emulator (<code>qemu-aarch64</code> or <code>qemu-arm</code>).</li> <li>Set <code>LSW_WINE_AARCH64</code> or <code>LSW_WINE_ARM</code> to an architecture-specific    Wine build.</li> <li>If that Wine needs its sysroot, set <code>QEMU_LD_PREFIX</code>.</li> </ol> <p>When an item is missing, <code>lsw run</code> fails with an error that names it. To test ARM64 output on real hardware, use <code>lsw verify --native</code>.</p>"},{"location":"guide/targets/#linking-static-or-dynamic","title":"Linking: static or dynamic","text":"<p>LSW links the C/C++ runtime statically by default. The binaries are self-contained. They need only the DLLs that all Windows 10 and later installations have: KERNEL32 and the UCRT api-sets.</p> <p>To link the shared mingw runtime, set <code>link = \"dynamic\"</code> in <code>[toolchain]</code>. LSW then copies the runtime DLLs that the binary imports next to the binary. Examples: <code>libstdc++-6.dll</code>, <code>libgcc_s_seh-1.dll</code>, <code>libwinpthread-1.dll</code>, and their transitive imports.</p>"},{"location":"guide/testing/","title":"Testing and debugging","text":""},{"location":"guide/testing/#run-tests-under-wine","title":"Run tests under Wine","text":"<p><code>lsw test</code> builds the project, then runs the test suite through the environment's Wine. LSW selects the test runner:</p> Project Runner CMake with tests <code>ctest</code> Rust <code>cargo test</code>, with a Wine target runner Meson (configured build) <code>meson test</code> Any, with <code>[test] command</code> in <code>lsw.toml</code> your command <p>The report separates the build result, the runtime result, and the native result. The verdict for a local pass is <code>LOCAL_COMPATIBILITY_VERIFIED</code>. This is a Wine pass. It is not a Windows pass.</p> <p>Options:</p> <ul> <li><code>--headless</code> - run GUI tests under a virtual display (<code>xvfb-run</code>) and   export <code>LSW_HEADLESS=1</code> to the suite.</li> <li><code>--junit &lt;file&gt;</code> - write a JUnit XML report (ctest and meson runs).</li> <li><code>--coverage</code> - instrument with clang source coverage and show an   llvm-cov report. This needs the llvm-mingw toolchain.</li> </ul> <p><code>lsw check</code> wraps the same execution into one validation pass: configuration, environment, build, Wine execution, DLL resolution, and PE hardening. When the project has no tests, <code>lsw check</code> does a smoke run of the console executable instead.</p>"},{"location":"guide/testing/#debug-under-wine","title":"Debug under Wine","text":"<ul> <li><code>lsw debug &lt;pe&gt;</code> opens the winedbg console.</li> <li><code>lsw debug &lt;pe&gt; --gdb</code> starts a gdb-remote stub. Attach host gdb or lldb   to it. Add <code>--no-start</code> to print the connection info only.</li> <li><code>lsw debug --attach &lt;pid&gt;</code> attaches to a process that runs in the   environment. <code>lsw ps</code> lists the process IDs.</li> <li><code>lsw dap</code> is a Debug Adapter Protocol server on stdio. The VS Code   extension uses it. <code>lsw ide launch-config</code> writes a ready   <code>.vscode/launch.json</code>.</li> </ul>"},{"location":"guide/testing/#debug-on-native-windows","title":"Debug on native Windows","text":"<p><code>lsw debug --native &lt;pe&gt;</code> runs the binary under cdb on the <code>[verify]</code> Windows host and returns a real backtrace. Add <code>--analyze</code> for <code>!analyze -v</code> triage, or <code>--interactive</code> for a live cdb session. See native verification for the host setup.</p>"},{"location":"guide/testing/#crash-dumps","title":"Crash dumps","text":"<ul> <li><code>lsw run --dump-on-crash</code> captures a minidump when the program fails,   and decodes it.</li> <li><code>lsw crash &lt;dump.dmp&gt;</code> decodes a Windows minidump: the exception, the   faulting module, and the address.</li> <li><code>lsw crash --force &lt;pe&gt;</code> launches the program and forces a dump without   a crash.</li> </ul>"},{"location":"guide/testing/#watch-mode","title":"Watch mode","text":"<p><code>lsw watch</code> builds again on each source change.</p> <ul> <li><code>--run</code> - after each good build, restart the project executable.</li> <li><code>--test</code> - after each good build, run the test suite again.</li> </ul>"},{"location":"guide/verify/","title":"Native Windows verification","text":"<p>A pass under Wine is not a pass on Windows. <code>lsw verify</code> gives the real answer. It builds the project, copies the binaries to a real Windows host, runs them there, and reports one of two results:</p> <ul> <li><code>WINDOWS_VERIFIED</code> - the binaries ran on Windows.</li> <li><code>WINDOWS_UNAVAILABLE</code> - no host was reachable. This is not a failure of   your binary. It is the absence of a verdict.</li> </ul>"},{"location":"guide/verify/#configure-the-host","title":"Configure the host","text":"<p>Add a <code>[verify]</code> section to <code>lsw.toml</code>:</p> <pre><code>[verify]\ntransport     = \"ssh\"            # ssh | winrm | https\nhost          = \"user@win-host\"\nidentity_file = \"~/.ssh/lsw_verify\"\n</code></pre> <ul> <li><code>ssh</code> needs an OpenSSH server on the Windows host.</li> <li><code>winrm</code> and <code>https</code> (WinRM over TLS) read the password from the   <code>LSW_WINRM_PASSWORD</code> environment variable.</li> <li><code>remote_dir</code> sets the remote work directory. <code>dump_dir</code> sets the remote   crash-dump directory. Both have transport defaults.</li> </ul>"},{"location":"guide/verify/#what-uses-the-host","title":"What uses the host","text":"<ul> <li><code>lsw verify [--native]</code> - run the artifacts and report the verdict.</li> <li><code>lsw compat --native</code> - add the real Windows verdict to a compatibility   report.</li> <li><code>lsw debug --native &lt;pe&gt;</code> - run under cdb for a real backtrace,   <code>!analyze -v</code> triage, or an interactive session. This path needs the   <code>ssh</code> transport.</li> </ul>"},{"location":"guide/verify/#reproducibility-verification","title":"Reproducibility verification","text":"<p><code>lsw verify --reproducible [artifact]</code> does not use the Windows host. It builds the project two times and makes sure that the artifacts are byte-identical. On a mismatch, it reports the sections that diverge.</p>"},{"location":"guide/windows-state/","title":"Shell, paths, and Windows state","text":""},{"location":"guide/windows-state/#the-two-views-of-your-project","title":"The two views of your project","text":"<p>Each environment mounts the project at <code>C:\\src\\&lt;name&gt;</code> on its drive C:. The same files are visible from Linux at the project root. LSW maps paths between the two views deterministically.</p> <ul> <li><code>lsw path --to-windows &lt;linux-path&gt;</code> prints the Windows form.</li> <li><code>lsw path --to-linux &lt;windows-path&gt;</code> prints the Linux form.</li> </ul>"},{"location":"guide/windows-state/#shells","title":"Shells","text":"<p><code>lsw shell</code> opens a Linux shell with the Windows-target variables set: <code>CC</code>, <code>CXX</code>, <code>LSW_ENV</code>, <code>LSW_PROJECT</code>, <code>LSW_TARGET_FLAGS</code>, and <code>WINEPREFIX</code>.</p> <p><code>lsw shell --windows</code> opens <code>cmd</code> (or <code>powershell.exe</code> when the prefix has it) in <code>C:\\src\\&lt;name&gt;</code>.</p> <p><code>lsw exec &lt;command&gt;</code> runs one command. Use <code>--windows</code> or <code>--host</code> to select the execution domain. <code>lsw run</code> selects the domain from the file type: a PE binary runs through Wine, an ELF binary runs natively.</p>"},{"location":"guide/windows-state/#registry","title":"Registry","text":"<p>Each environment has its own registry. <code>lsw registry</code> (alias <code>reg</code>) manages it:</p> <ul> <li><code>get &lt;key&gt; [value]</code> - read a key.</li> <li><code>set &lt;key&gt; &lt;value&gt; &lt;data&gt; [--type string|dword|expand]</code> - write a value.</li> <li><code>export &lt;key&gt; &lt;file&gt;</code> / <code>import &lt;file&gt;</code> - move keys through <code>.reg</code>   files.</li> <li><code>seed</code> - apply the <code>[[registry.seed]]</code> entries from <code>lsw.toml</code>. Use this   to give tests a declarative starting state.</li> <li><code>reset</code> - discard all registry state and rebuild the prefix defaults.</li> </ul>"},{"location":"guide/windows-state/#processes-and-services","title":"Processes and services","text":"<ul> <li><code>lsw ps</code> lists the processes that run in the environment. Wine   infrastructure is hidden. Add <code>--all</code> to show it.</li> <li><code>lsw kill &lt;pid&gt;</code> sends SIGTERM to one process. <code>lsw kill --all</code> shuts   the environment down.</li> <li><code>lsw service create|start|stop|query|delete</code> manages Windows services   in the environment. The <code>service</code> project template gives you a service   skeleton to start from.</li> </ul> <p>Note: the global <code>--env &lt;name&gt;</code> flag points these commands at a named environment. With it, they work outside a project directory.</p>"},{"location":"guide/windows-state/#the-daemon-and-plugins","title":"The daemon and plugins","text":"<p><code>lswd</code> is an optional background daemon, managed with <code>lsw daemon start|status|stop</code>. It answers version and ping probes only. Normal use does not need it.</p> <p><code>lsw plugin list</code> discovers <code>lsw-provider-*</code> executables on <code>PATH</code> and shows their handshake information. The plugin surface is experimental. Builds do not consult providers yet.</p>"},{"location":"reference/commands/","title":"Command reference","text":"<p>Most report commands accept <code>--format json</code> for machine consumption. Global flags: <code>--verbose</code>, <code>--trace</code>, <code>--format human|json|csv</code>, and <code>--env &lt;name&gt;</code> (use a named environment instead of the project's active one; lets <code>ps</code>, <code>kill</code>, <code>registry</code>, and <code>service</code> run outside a project directory). Commands with no machine format print a note to stderr when <code>--format json|csv</code> is requested.</p> <p>The <code>lsw.toml</code> schema and every <code>LSW_*</code> environment variable are in configuration.md. Failure codes are catalogued in ../troubleshooting.md.</p>"},{"location":"reference/commands/#getting-started","title":"Getting started","text":"<ul> <li><code>lsw setup</code> detects the project (CMake, Meson, Cargo, Zig, Make, .NET),   creates a default environment for the <code>[target]</code> arch if none is active   (<code>windows-x64</code> for the default <code>x86_64</code>), writes <code>lsw.toml</code> and a   <code>.gitignore</code> if missing, and prints the next command.</li> <li><code>lsw init [name] [--template console|cpp|gui|dll|service]</code> scaffolds a new   project (<code>lsw.toml</code> + CMake template). The default is <code>console</code> (C).   <code>cpp</code> is a C++ console app, <code>gui</code> uses WinMain, <code>dll</code> makes a shared   library, and <code>service</code> is a Windows service skeleton that pairs with   <code>lsw service create|start|stop</code>.</li> <li><code>lsw doctor</code> diagnoses host, runtime, toolchain, and project health.</li> <li><code>lsw check [--headless]</code> validates the project in one pass: configuration,   environment, build, wine execution (tests or a console smoke run),   DLL-dependency resolution, and baseline PE hardening. Nonzero exit on any   failure.</li> </ul>"},{"location":"reference/commands/#build-run","title":"Build / run","text":"<ul> <li><code>lsw build [--system cmake|cargo|make|ninja|meson|zig|dotnet|explicit]   [--reproducible] [--update-lock] [--aot]</code> builds the project; <code>explicit</code>   forces the <code>[build]</code> command from <code>lsw.toml</code>. An unknown <code>--system</code> value   is an error (LSW2046), never silently ignored.</li> <li><code>lsw run [--host|--windows] [--sandbox strict] [--headless]   [--dump-on-crash] [program]</code> starts a program; omit the program to build and   run the project's single executable.</li> <li><code>lsw exec [--host|--windows] &lt;cmd&gt;</code> runs one command in an explicit domain.</li> <li><code>lsw test [--headless] [--junit &lt;file&gt;] [--coverage]</code> runs the tests and   shows the true compatibility status. ctest, <code>cargo test</code> (run under Wine   via a target runner), <code>meson test</code>, and an explicit <code>[test]</code> command are   supported; <code>--junit</code> writes a JUnit XML report for ctest and meson runs;   <code>--coverage</code> instruments with clang source coverage and prints an   llvm-cov report (llvm-mingw toolchain only).</li> <li><code>lsw shell [--windows]</code> opens a shell (cmd.exe with <code>--windows</code>).</li> <li><code>lsw watch [--run|--test]</code> rebuilds automatically when source files change;   <code>--run</code> restarts the project executable after each build, <code>--test</code> reruns   the suite.</li> <li><code>lsw clean [--deps]</code> removes <code>build/</code> and <code>dist/</code> (and <code>deps/</code> with   <code>--deps</code>).</li> </ul>"},{"location":"reference/commands/#binary-analysis","title":"Binary analysis","text":"<p>Analysis commands accept an explicit file, or omit it to build and use the project's artifact.</p> <ul> <li><code>lsw inspect [pe]</code> shows format, architecture, subsystem, entry point,   sections, embedded manifest/version-info/icon, and imports.</li> <li><code>lsw audit [pe]</code> reports hardening (ASLR, DEP, CFG, SafeSEH, signing) as   enabled, disabled, or not applicable to the target architecture.</li> <li><code>lsw exports [dll]</code> lists exported symbols.</li> <li><code>lsw deps tree [pe]</code> shows the transitive DLL tree.</li> <li><code>lsw diff &lt;a.exe&gt; &lt;b.exe&gt;</code> compares imports, exports, sections, and size.</li> <li><code>lsw size [pe] [--baseline &lt;pe&gt;] [--max-growth &lt;pct&gt;]</code> breaks down size by   section bucket.</li> <li><code>lsw strings [file] [--min &lt;n&gt;]</code> lists printable ASCII/UTF-16 strings.</li> <li><code>lsw sbom [pe]</code> writes a CycloneDX SBOM.</li> <li><code>lsw crash &lt;dump.dmp&gt;</code> decodes a Windows minidump (exception, module,   address). <code>lsw crash --force &lt;pe&gt;</code> launches the program and forces a dump.</li> </ul>"},{"location":"reference/commands/#dependencies","title":"Dependencies","text":"<ul> <li><code>lsw deps add|remove &lt;name&gt;</code> installs or removes prebuilt mingw-w64   libraries (headers, import/static libraries, DLLs) in the project <code>deps/</code>   sysroot and records them in <code>[dependencies]</code>; <code>lsw deps list</code> shows what is   installed. Builds find the include and library paths automatically.   <code>lsw deps tree [pe]</code> (under Binary analysis) is unrelated: it shows a   binary's DLL import tree.</li> </ul>"},{"location":"reference/commands/#compatibility-and-tracing","title":"Compatibility and tracing","text":"<ul> <li><code>lsw compat [--db] [--native] [pe]</code> produces a measured report of imported   DLLs, imported API functions, and the runtime trace. <code>--native</code> adds a real   Windows verdict from the <code>[verify]</code> host. Results are scoped: local numbers   reflect the Wine runtime, not native Windows.</li> <li><code>lsw compat-query &lt;dll|module!func&gt;</code> queries the persistent compatibility   database.</li> <li><code>lsw trace [--relay] [--filter &lt;glob&gt;] [pe]</code> shows DLL loads, registry and   filesystem access, and any unsupported APIs observed in that Wine execution,   plus an event timeline (<code>--format csv|json</code>).</li> </ul>"},{"location":"reference/commands/#debugging","title":"Debugging","text":"<ul> <li><code>lsw debug &lt;pe&gt; [--gdb [--no-start]]</code> starts winedbg or a gdb-remote stub   that gdb/lldb can attach to. <code>lsw debug --attach &lt;pid&gt; [--gdb]</code> attaches to   a process already running in the environment (<code>lsw ps</code> lists pids).</li> <li><code>lsw debug --native &lt;pe&gt; [--analyze|--interactive]</code> runs the binary under   cdb on the <code>[verify]</code> Windows host for a real backtrace, <code>!analyze -v</code>   automation, or an interactive session.</li> <li><code>lsw dap</code> runs a Debug Adapter Protocol server on stdio for IDEs.</li> </ul>"},{"location":"reference/commands/#packaging-and-signing","title":"Packaging and signing","text":"<ul> <li><code>lsw package [--target portable-directory|zip|msi|msix|nsis|winget]   [--verify] [--bundle-deps]</code> assembles a distributable. <code>--bundle-deps</code>   copies each artifact's non-system DLL closure into the package.   <code>--target msi --verify</code> install-tests the MSI in a scratch environment.   <code>nsis</code> builds an NSIS setup.exe (needs <code>makensis</code>); <code>winget</code> builds the MSI   plus a winget manifest trio (needs <code>[package] installer_url</code>). With   <code>[package] shortcuts = true</code> the MSI and NSIS installers create Start-menu   shortcuts.</li> <li><code>lsw sign &lt;pe&gt; [--publisher &lt;subject&gt;] [--pfx &lt;path&gt; --pfx-pass-env &lt;VAR&gt;]   [--timestamp-url &lt;url&gt;]</code> adds an Authenticode signature (self-signed dev   identity by default). <code>lsw sign &lt;pe&gt; --verify</code> checks an existing   signature with osslsigncode.</li> </ul>"},{"location":"reference/commands/#native-verification","title":"Native verification","text":"<ul> <li><code>lsw verify [--native]</code> builds, then runs the artifacts on a real   Windows host configured in <code>[verify]</code> (SSH, WinRM, or WinRM-over-TLS;   WS-Man transports read <code>LSW_WINRM_PASSWORD</code>). Native verification is the   default; <code>--native</code> (alias <code>--native-windows</code>) is accepted for   explicitness. Result is <code>WINDOWS_VERIFIED</code> or <code>WINDOWS_UNAVAILABLE</code> -   deliberately distinct from local Wine results.</li> <li><code>lsw verify --reproducible [artifact]</code> builds twice and proves the artifacts   are byte-identical, reporting diverging sections on mismatch.</li> </ul>"},{"location":"reference/commands/#environment-state","title":"Environment state","text":"<ul> <li><code>lsw env create &lt;name&gt; [--arch &lt;arch&gt;] [--toolchain &lt;id&gt;] [--sdk &lt;name&gt;]   [--force] [--expose-home]</code> creates an isolated environment (Wine prefix +   toolchain probe); <code>lsw env list|clone|restore|remove</code> manage them.   <code>lsw use &lt;name&gt;</code> selects the active one.</li> <li><code>lsw registry get|set|seed|export|import|reset</code> (alias <code>reg</code>) manages the   environment's isolated registry.</li> <li><code>lsw ps [--all]</code>, <code>lsw kill &lt;pid&gt;|--all</code> manage runtime processes.</li> <li><code>lsw service create|start|stop|query|delete</code> manages Windows services.</li> <li><code>lsw sdk import|list|remove</code> manages user-supplied Windows SDK sysroots for   MSVC-ABI builds. <code>lsw sdk acquire &lt;name&gt; --accept-license</code> downloads one   via xwin under the Microsoft license.</li> <li><code>lsw deps vendor &lt;dir&gt;</code> copies a prebuilt <code>include/</code>/<code>lib/</code>/<code>bin/</code> tree   (e.g. a vcpkg or Conan export) into the project <code>deps/</code> sysroot.</li> <li><code>lsw env export &lt;name&gt; &lt;file.tar.zst&gt;</code> / <code>lsw env import-archive &lt;name&gt;   &lt;file&gt; [--force]</code> pack and restore whole environments (CI cache handoff).</li> <li><code>lsw toolchain install llvm-mingw[@version]</code> downloads a self-contained   llvm-mingw release into <code>~/.local/share/lsw/toolchains</code> (default: latest);   managed toolchains are found automatically by <code>lsw env create</code>, newest   first. <code>lsw toolchain list|remove</code> manage them.</li> <li><code>lsw env provision winetricks &lt;verbs...&gt;</code> runs winetricks against the   environment's prefix (e.g. <code>dxvk</code>, <code>vkd3d</code>, <code>corefonts</code>).</li> <li><code>lsw path --to-windows &lt;linux-path&gt;|--to-linux &lt;windows-path&gt;</code> converts a path between views (old <code>--windows</code>/<code>--linux</code> spellings remain as aliases).</li> </ul>"},{"location":"reference/commands/#languages","title":"Languages","text":"<ul> <li><code>lsw rust init|doctor</code> scaffolds and diagnoses Rust-&gt;Windows projects.</li> <li><code>lsw dotnet init|doctor</code> scaffolds and diagnoses C#/.NET projects.</li> </ul>"},{"location":"reference/commands/#integration-and-tooling","title":"Integration and tooling","text":"<ul> <li><code>lsw ide env</code> prints the environment description IDE plugins consume. The   VS Code, Neovim, and JetBrains front-ends are in <code>editors/</code>.   <code>lsw ide launch-config</code> writes a ready <code>.vscode/launch.json</code>.</li> <li><code>lsw plugin list</code> discovers out-of-process <code>lsw-provider-*</code> JSON-RPC   providers on <code>PATH</code> (reference implementation in   <code>crates/lsw-provider-example</code>). The plugin surface is experimental:   providers are discovered and handshaked but not yet consulted by build or   runtime resolution.</li> <li><code>lswd</code> is an optional background daemon managed with   <code>lsw daemon start|status|stop</code>. It currently only answers version/ping   probes; normal use does not need it.</li> <li><code>lsw ci init [github|gitlab]</code> writes a GitHub Actions workflow or a   GitLab CI pipeline.</li> <li><code>lsw config check</code> lints <code>lsw.toml</code>.</li> <li><code>lsw explain LSW2004</code> explains an error code.</li> <li><code>lsw completions bash|zsh|fish|powershell|elvish</code> writes shell completions;   <code>lsw man [--dir &lt;out&gt;]</code> writes man pages; <code>lsw install [--prefix &lt;dir&gt;]</code>   installs bash/zsh/fish completions and man pages (default prefix   <code>$PREFIX</code>, else <code>~/.local</code>).</li> </ul>"},{"location":"reference/configuration/","title":"Configuration reference","text":""},{"location":"reference/configuration/#lswtoml","title":"<code>lsw.toml</code>","text":"<p>One file per project, discovered by walking up from the working directory. A generated manifest contains only <code>[project]</code>; every other section is optional. Unknown keys are hard errors, so typos fail loudly instead of being ignored. Commit <code>lsw.toml</code> and <code>lsw.lock</code> to version control.</p>"},{"location":"reference/configuration/#project","title":"<code>[project]</code>","text":"key required meaning <code>name</code> yes project name; also the artifact and package base name"},{"location":"reference/configuration/#target","title":"<code>[target]</code>","text":"key default meaning <code>arch</code> <code>x86_64</code> <code>x86_64</code>, <code>x86</code>, <code>aarch64</code>, <code>armv7</code>, or <code>arm64ec</code>; <code>lsw setup</code> creates the default environment for this arch <code>api</code> none minimum Windows API level (<code>win7</code>, <code>win8</code>, <code>win10</code>, <code>win11</code>); sets <code>_WIN32_WINNT</code>, <code>WINVER</code>, <code>NTDDI_VERSION</code> <code>os</code> <code>windows</code> only <code>windows</code> is accepted"},{"location":"reference/configuration/#toolchain","title":"<code>[toolchain]</code>","text":"key default meaning <code>link</code> <code>static</code> <code>static</code> links the C/C++ runtime into the artifact; <code>dynamic</code> deploys the mingw runtime DLLs next to it <code>aot</code> <code>false</code> compile C# with NativeAOT (native PE, no CLR) <code>ccache</code> <code>false</code> wrap CMake compilation in ccache when it is installed <code>provider</code> none reserved; toolchain selection lives on the environment (<code>lsw env create --toolchain</code>) <code>version</code> none reserved; pinning lives in <code>lsw.lock</code>"},{"location":"reference/configuration/#runtime","title":"<code>[runtime]</code>","text":"key default meaning <code>provider</code> <code>wine</code> reserved; the runtime is resolved per environment <code>version</code> none reserved; pinning lives in <code>lsw.lock</code>"},{"location":"reference/configuration/#environment","title":"<code>[environment]</code>","text":"key default meaning <code>name</code> none the active environment; written by <code>lsw use</code> and <code>lsw setup</code>"},{"location":"reference/configuration/#filesystem","title":"<code>[filesystem]</code>","text":"key default meaning <code>project_drive</code> <code>C:</code> only <code>C:</code> is supported today <code>mount_project</code> <code>/src</code> only <code>/src</code> is supported today; the project mounts at <code>C:\\src\\&lt;name&gt;</code> <code>case</code> <code>native</code> <code>strict</code> turns case-insensitive filename collisions into build errors"},{"location":"reference/configuration/#build-test","title":"<code>[build]</code> / <code>[test]</code>","text":"key meaning <code>command</code> explicit argv, e.g. <code>[\"make\", \"-f\", \"windows.mk\"]</code>; skips build-system detection. The command runs with the cross <code>CC</code>, <code>CXX</code>, <code>CFLAGS</code>, <code>LDFLAGS</code> exported"},{"location":"reference/configuration/#env","title":"<code>[env]</code>","text":"<pre><code>[env.vars]           # extra Windows environment variables for run/exec/test\nRUST_LOG = \"debug\"\n[env.secret]         # inject a host variable by name; the value stays out of the manifest\nAPI_TOKEN = \"HOST_API_TOKEN\"\n</code></pre>"},{"location":"reference/configuration/#registryseed","title":"<code>[[registry.seed]]</code>","text":"<p>Registry values applied by <code>lsw registry seed</code>:</p> <pre><code>[[registry.seed]]\nkey   = \"HKCU\\\\Software\\\\Hello\"\nname  = \"FirstRun\"\nvalue = \"1\"\ntype  = \"dword\"      # string (default) | dword | expand\n</code></pre>"},{"location":"reference/configuration/#sandbox","title":"<code>[sandbox]</code>","text":"key default meaning <code>network</code> <code>host</code> <code>host</code>, <code>isolated</code> (NAT via pasta), or <code>none</code> <code>cpu_seconds</code> none rlimit for <code>lsw run --sandbox strict</code> <code>memory_mb</code> none rlimit for <code>lsw run --sandbox strict</code>"},{"location":"reference/configuration/#verify","title":"<code>[verify]</code>","text":"key default meaning <code>transport</code> <code>ssh</code> <code>ssh</code>, <code>winrm</code>, or <code>https</code> <code>host</code> none native Windows verification host, e.g. <code>user@win-host</code> <code>identity_file</code> none ssh identity for the verification host <code>remote_dir</code> transport default remote directory artifacts are copied to <code>dump_dir</code> transport default remote directory crash dumps are read from"},{"location":"reference/configuration/#package","title":"<code>[package]</code>","text":"<p>Distribution metadata consumed by <code>lsw package</code> (MSI/MSIX) and embedded into the built PE as an icon, VERSIONINFO resource, and application manifest (needs a <code>windres</code>; llvm-mingw and mingw-w64 both ship one):</p> key meaning <code>version</code> product version, e.g. <code>\"2.5.1\"</code> (MSI Version, MSIX Identity, PE FILEVERSION) <code>publisher</code> company name (MSI Manufacturer, MSIX Publisher CN, PE CompanyName) <code>description</code> PE FileDescription and MSIX description <code>icon</code> project-relative <code>.ico</code> for the PE icon (a <code>.png</code> is used as the MSIX logo instead) <code>upgrade_code</code> explicit MSI UpgradeCode GUID (default: derived deterministically from the project name) <code>shortcuts</code> <code>true</code> adds Start-menu shortcuts for each .exe to the MSI and NSIS installers <code>installer_url</code> base URL where you will host the installer; required by <code>lsw package --target winget</code> <code>dpi_aware</code> embed a dpiAware application manifest (<code>true</code> = per-monitor) <code>requires_admin</code> embed <code>requireAdministrator</code> in the application manifest"},{"location":"reference/configuration/#dependencies","title":"<code>[dependencies]</code>","text":"<p>Prebuilt Windows libraries fetched by <code>lsw deps add &lt;name&gt;</code> from the MSYS2 mingw package repos (sha256-verified, cached under <code>~/.cache/lsw</code>):</p> <pre><code>[dependencies]\nzlib = \"1.3.1-1\"\n</code></pre>"},{"location":"reference/configuration/#environment-variables-lsw-reads","title":"Environment variables LSW reads","text":"variable meaning <code>LSW_WINE</code> absolute path to the wine binary to use instead of <code>wine</code> from <code>PATH</code> (WineHQ <code>/opt</code> builds, Proton, Nix) <code>LSW_TOOLCHAIN_DIRS</code> colon-separated directories searched for cross toolchains before <code>PATH</code> (e.g. an extracted llvm-mingw <code>bin/</code>) <code>LSW_WINE_X86_64</code> / <code>LSW_WINE_X86</code> / <code>LSW_WINE_AARCH64</code> / <code>LSW_WINE_ARM</code> per-architecture Wine builds for qemu user-mode emulation of cross-family targets <code>LSW_WINRM_PASSWORD</code> password for <code>[verify] transport = \"winrm\"</code> <code>LSW_PFX_B64</code> / <code>LSW_PFX_PASSWORD</code> signing certificate (base64 PKCS#12) and passphrase for CI signing <code>NO_COLOR</code> disable colored output"},{"location":"reference/configuration/#environment-variables-lsw-sets-for-child-processes","title":"Environment variables LSW sets for child processes","text":"<p>Build and test commands (and programs run through <code>lsw run</code>/<code>lsw exec</code>) see:</p> variable meaning <code>LSW_ENV</code> name of the active environment <code>LSW_PROJECT</code> absolute host path of the project root <code>LSW_TARGET_FLAGS</code> the cross C flags LSW passed to the toolchain <code>LSW_HEADLESS</code> set to <code>1</code> by <code>lsw test --headless</code> / <code>lsw check --headless</code>"},{"location":"reference/configuration/#user-configuration-file","title":"User configuration file","text":"<p><code>~/.config/lsw/config.toml</code>:</p> key meaning <code>default_environment</code> environment used when a project has no <code>[environment] name</code>"}]}